version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - bodyclose
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
